# Requirements file for SQL drivers with forced compilation from source
# This forces pip to compile from source instead of using binary wheels
# Useful when binary wheels are not compatible with Python 3.13

# SQL Database drivers - compile from source
--no-binary pyodbc
pyodbc>=5.0.0

--no-binary pymssql  
pymssql>=2.2.8

# Azure Functions and other dependencies - use binary wheels (faster)
azure-functions>=1.11.0
azure-identity>=1.12.0
azure-keyvault-secrets>=4.7.0
pandas>=1.5.0
requests>=2.28.0
python-dateutil>=2.8.0
